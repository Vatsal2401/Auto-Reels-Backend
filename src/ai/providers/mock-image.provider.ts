import { Injectable } from '@nestjs/common';
import { IImageGenerator } from '../interfaces/image-generator.interface';

@Injectable()
export class MockImageProvider implements IImageGenerator {
  async generateImage(prompt: string): Promise<Buffer> {
    console.warn('⚠️  Mock Image Provider: Returning placeholder image. Set OPENAI_API_KEY for real image generation.');
    
    // Return a minimal 1x1 PNG image (transparent)
    // This is a valid PNG file that can be used for testing
    const pngBuffer = Buffer.from([
      0x89, 0x50, 0x4e, 0x47, 0x0d, 0x0a, 0x1a, 0x0a, // PNG signature
      0x00, 0x00, 0x00, 0x0d, // IHDR chunk length
      0x49, 0x48, 0x44, 0x52, // IHDR
      0x00, 0x00, 0x00, 0x01, // Width: 1
      0x00, 0x00, 0x00, 0x01, // Height: 1
      0x08, 0x06, 0x00, 0x00, 0x00, // Bit depth, color type, etc.
      0x1f, 0x15, 0xc4, 0x89, // CRC
      0x00, 0x00, 0x00, 0x0a, // IDAT chunk length
      0x49, 0x44, 0x41, 0x54, // IDAT
      0x78, 0x9c, 0x63, 0x00, 0x01, 0x00, 0x00, 0x05, 0x00, 0x01, // Compressed data
      0x0d, 0x0a, 0x2d, 0xb4, // CRC
      0x00, 0x00, 0x00, 0x00, // IEND chunk length
      0x49, 0x45, 0x4e, 0x44, // IEND
      0xae, 0x42, 0x60, 0x82, // CRC
    ]);
    
    return pngBuffer;
  }
}
